# Illustrative Knative skeleton only — NOT a full production manifest.
# Do NOT apply with placeholder passwords. Configure env + Secret Manager in Cloud Run console.
# Deploying with only APP_* below would omit SMTP_*, TRACKING_PUBLIC_ORIGIN, Firestore, etc.
apiVersion: serving.knative.dev/v1
kind: Service
metadata:
  name: wrrapd-tracking
spec:
  template:
    spec:
      containerConcurrency: 80
      timeoutSeconds: 300
      containers:
        - image: gcr.io/PROJECT_ID/wrrapd-tracking
